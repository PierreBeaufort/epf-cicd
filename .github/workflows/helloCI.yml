name: Hello CI
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Say-Hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello CI/CD"
      - run: echo "Author is ${{ github.actor }} on ${{ github.ref }}"
