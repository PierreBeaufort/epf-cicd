# EPF CI/CD - Pierre Beaufort

1. Qu'est-ce qui déclenche la pipeline ? Pouvez-vous la déclencher autrement ? > Push, peut-être déclenché par build ou autre
2. Où s'exécute-t-elle ? Sur quel système d'exploitation ? Qui fournit cette machine ? > OS ubuntu fournit par github
3. Que se passe-t-il si une commande échoue ? Le step suivant s'exécute-t-il ? Comment
l'avez-vous vérifié ? > Dans action, en cas d'erreur il est impossible de voir de résultat.
4. À quoi cela pourrait-il servir sur un vrai projet ? Donnez trois exemples de commandes
utiles. > Cela peut servir à avoir un récapitulatif de ce qui a été fait sur github et de le partager avec des participants.
5. Qu'est-ce qui vous semble compliqué ? Qu'est-ce qui vous semble « magique » ? > Compliqué de voir exactement ce que l'on cherche dans la doc.
